# VEDA Kendra — Custom passkey hash
# Generate your own hash by running this in the browser console:
#   const buf = await crypto.subtle.digest('SHA-256', new TextEncoder().encode('your-passkey-here'));
#   console.log(Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2,'0')).join(''));
#
# Then paste the output below and rebuild.
# Default passkey (if this var is not set): VedaKendra@2024

VITE_KENDRA_HASH=paste-your-sha256-hash-here
